import { ReactNode } from "react";

export type RouterType = {
  element: ReactNode;
  path: string;
  // icon: ReactNode;
  // iconClosed: ReactNode;
  // iconOpen: ReactNode;
  // subRouter: RouterType;
};
