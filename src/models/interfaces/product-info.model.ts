export interface Source {
    sourceId: number;
    sourceName: string;
}

export type TypeProduct = {
    typeId: number,
    typeName: string
};

export type Color = {
    colorId: number,
    colorName: string,
    colorCode: string,
};

export type Size = {
    sizeId: number,
    size: string,
}

